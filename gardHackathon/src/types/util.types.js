/**
* @typedef {Object.<string, (boolean|undefined|null)>} IElementClasses
*/

/**
 * Пример использования IElementClasses
 *
 * @type {IElementClasses}
 */
export const IElementClasses = {
    className1: true,       // Существующий класс
    className2: false,      // Не существующий класс
    className3: undefined,  // Не существующий класс
    className4: null        // Не существующий класс
};